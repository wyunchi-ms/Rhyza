# Use the tarball URL returned by the selected registry without rewriting its path.
replace-registry-host=never
# Keep the lockfile portable between public registries and corporate mirrors.
omit-lockfile-registry-resolved=true
